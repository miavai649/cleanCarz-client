import { TService } from './service.type'

export type TSlot = {
  service: TService
  date: string
  startTime: string
  endTime: string
  isBooked: string
  _id: string
  createdAt: string
  updatedAt: string
}
